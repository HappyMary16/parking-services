package org.example.controller;

public class StatisticController {

    /*
     * TODO:
     * Implement a class StatisticController.
     * It must process next requests:
     * - Get statistics for all parking
     *   /statistic
     * - Get a statistic for a specific parking.
     *   Parking name must be specified in a request param
     *   /statistic
     *   Request URL example: /statistic?parkingName=Java
     */
}
